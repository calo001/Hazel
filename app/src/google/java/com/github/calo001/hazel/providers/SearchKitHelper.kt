package com.github.calo001.hazel.providers

import android.content.Context

class SearchKitHelper(private val context: Context) {
    fun searchImage(token: String, queryArg: String): Any {
        return Unit
    }
}